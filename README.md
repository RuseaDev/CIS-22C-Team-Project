## Execute commands

```
make                  # Compiles the project
make run              # Compiles and runs the project
make clean            # Removes the compiled executable
./program             # Runs the project (no recompiling)
```